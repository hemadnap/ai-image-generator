#!/bin/bash

# Display backend project structure

cat << 'EOF'

📁 BACKEND PROJECT STRUCTURE
════════════════════════════════════════════════════════════════════════════

backend/
│
├── 📄 Configuration Files
│ ├── wrangler.toml ← Cloudflare Worker configuration
│ ├── tsconfig.json ← TypeScript configuration
│ ├── package.json ← Project dependencies
│ └── .env.example ← Environment variables template
│
├── 📁 src/
│ │
│ ├── 📄 index.ts ← Main entry point (request handler)
│ │
│ ├── 📁 router/
│ │ └── router.ts ← Request routing logic
│ │
│ ├── 📁 routes/ ← Endpoint definitions (path routing)
│ │ ├── auth.ts ← /api/v1/auth/_ endpoints
│ │ ├── users.ts ← /api/v1/users/_ endpoints
│ │ └── data.ts ← /api/v1/data/\* endpoints
│ │
│ ├── 📁 handlers/ ← Business logic for each endpoint
│ │ ├── authHandler.ts ← Auth operations (login, logout, etc.)
│ │ ├── userHandler.ts ← User operations (CRUD)
│ │ └── dataHandler.ts ← Dashboard & analytics data
│ │
│ ├── 📁 services/ ← External integrations & complex logic
│ │ └── googleService.ts ← Google OAuth token verification
│ │
│ ├── 📁 middleware/ ← Cross-cutting concerns
│ │ ├── cors.ts ← CORS handling
│ │ ├── errorHandler.ts ← Global error handling
│ │ └── requestLogger.ts ← Request logging
│ │
│ ├── 📁 utils/ ← Helper functions
│ │ ├── responses.ts ← Response builders (success/error)
│ │ ├── auth.ts ← Auth utilities (token extraction)
│ │ └── jwt.ts ← JWT creation and verification
│ │
│ └── 📁 types/ ← TypeScript type definitions
│ └── index.ts ← All interfaces and types
│
├── 📄 Documentation
│ ├── README.md ← Full API documentation
│ ├── ARCHITECTURE.md ← System design & data flow
│ ├── DEPLOYMENT.md ← Deployment instructions
│ └── QUICKSTART.md ← Quick start guide
│
└── 📁 Other Files
├── .gitignore ← Git ignore patterns
└── package-lock.json ← Dependency lock file

════════════════════════════════════════════════════════════════════════════

📡 API ENDPOINTS HIERARCHY
════════════════════════════════════════════════════════════════════════════

/api/v1/
│
├── auth/
│ ├── POST /google ← Google OAuth login
│ ├── GET /me ← Get current user (protected)
│ ├── POST /logout ← Logout
│ └── POST /refresh ← Refresh token (protected)
│
├── users/
│ ├── GET / ← List all users
│ ├── GET /:id ← Get user by ID
│ └── PUT /:id ← Update user (protected)
│
└── data/
├── GET /dashboard ← Dashboard statistics
└── GET /analytics ← Analytics data

════════════════════════════════════════════════════════════════════════════

🏗️ LAYER ARCHITECTURE
════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│ HTTP REQUEST │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ index.ts (Entry Point) │
│ - Initialize middleware pipeline │
│ - Handle CORS, logging, errors │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ router.ts (Request Router) │
│ - Parse URL and method │
│ - Route to appropriate handler │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ routes/ (Route Definitions) │
│ - auth.ts, users.ts, data.ts │
│ - Map endpoints to handlers │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ handlers/ (Business Logic) │
│ - authHandler, userHandler, dataHandler │
│ - Implement endpoint functionality │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ services/ (External Integrations) │
│ - googleService (OAuth verification) │
│ - Business logic & complex operations │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ Database/Storage (KV or D1) │
│ - USERS_KV: User data │
│ - SESSIONS_KV: Session tokens │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ responses.ts (Response Building) │
│ - Format successful responses │
│ - Format error responses │
└─────────────────────────────────────────────────────────────┘
│
▼
┌─────────────────────────────────────────────────────────────┐
│ HTTP RESPONSE │
└─────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════

📦 KEY TECHNOLOGY STACK
════════════════════════════════════════════════════════════════════════════

Runtime: Cloudflare Workers (Serverless)
Language: TypeScript
Framework: Custom (Lightweight)
Build Tool: Wrangler
Package Manager: npm

Deployment: Cloudflare Workers
Database: KV (key-value) + Optional D1 (SQL)
Authentication: JWT + Google OAuth 2.0
CORS: Global distribution

════════════════════════════════════════════════════════════════════════════

🚀 QUICK COMMANDS
════════════════════════════════════════════════════════════════════════════

npm run dev ← Start local development server
npm run deploy ← Deploy to production
npm run deploy:staging ← Deploy to staging environment
npm run type-check ← Verify TypeScript types
npm run lint ← Check code quality
npm run tail ← View live logs

════════════════════════════════════════════════════════════════════════════

📚 DOCUMENTATION GUIDE
════════════════════════════════════════════════════════════════════════════

Start Here: QUICKSTART.md (5 min read)
API Reference: README.md (API endpoints & setup)
Architecture: ARCHITECTURE.md (System design)
Deployment: DEPLOYMENT.md (Production deployment)

════════════════════════════════════════════════════════════════════════════

EOF
